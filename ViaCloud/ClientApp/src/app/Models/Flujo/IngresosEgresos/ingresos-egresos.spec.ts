import { IngresosEgresos, TotalesIngresosEgresos } from './ingresos-egresos';

describe('IngresosEgresos', () => {
  it('should create an instance', () => {
    expect(new IngresosEgresos()).toBeTruthy();
  });
  it('should create an instance', () => {
    expect(new TotalesIngresosEgresos()).toBeTruthy();
  });
});
